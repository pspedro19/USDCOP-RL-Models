'use client';

/**
 * ORDER BOOK COMPONENT - COMPLETELY REMOVED FROM UI
 *
 * This component was displaying 100% simulated data using Math.random()
 * It has been completely hidden from the UI.
 *
 * USD/COP is an OTC forex pair - no public order book exists.
 *
 * Alternative implemented: VolumeProfileChart.tsx (uses real OHLCV data)
 * located at: /api/market/volume-profile
 */

export default function OrderBook() {
  // Return null - component completely hidden from UI
  // No visual noise, no explanations, nothing rendered
  return null;
}
