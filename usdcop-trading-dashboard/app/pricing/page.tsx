'use client';

/**
 * /pricing — public tier comparison on the GlobalMarkets design system
 * (CTR-GM-UI-001; CTR-RBAC-001 §B.5). Previous page archived at /legacy/pricing
 * (components/legacy/PricingLegacy.tsx, admin-only).
 */
import { PricingView } from '@/components/gm/views/PricingView';

export default function PricingPage() {
  return <PricingView />;
}
