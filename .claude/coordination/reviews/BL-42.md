commit: c4473f8
paths: test_return_units.py + generate_interpretability.py + tests + 3 JSONs
resultados: 17p+1s / 5-5p; delta BASELINE 0

---

## REVISIÓN CRUZADA CODEX — 2026-07-28

veredicto_revision: `APROBADO_PARCIAL`
corte_inmutable: `955374d0788f06f3f3b4dd946dd5e91060afb501`
cierre_integral_BL: `NO`

Evidencia ejecutada por CODEX:

- Corte inmutable: **28 passed / 3 DB skipped**.
- Mutación del productor
  `round(total_return, 2) -> round(total_return / 100.0, 6)`:
  **2 failed / 26 passed**.
- Blob productor restaurado exactamente a
  `4a974516becb77eedefd53414e978984f5418b1e`.
- Con PostgreSQL real y `BL42_REQUIRE_DB=1`:
  **30 passed / 1 xfailed estricto**; el canario DB sí se ejecutó.
- Con `--runxfail`, la deuda real produjo **1 fallo** y enumeró ocho columnas
  `_pct` con decimales disfrazados en executions, paper_trading, signals y
  subtrades.

La revisión del candado productor y del detector DB queda aprobada. BL-42
permanece `PARTIAL`: la base viva aún incumple la convención y requiere migrar
esas columnas a `*_decimal` o almacenar puntos porcentuales reales; no se puede
convertir un xfail que confirma el defecto en cierre del backlog.
