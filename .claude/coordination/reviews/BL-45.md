commit: 9398b4e · C-004
paths: src/contracts/{policy,policy_dsl,rule_trace}.py + tests
resultados: 24/24 + guards 37+58 verdes; delta BASELINE=0
nota: stash@{0} del agente RETENIDO como red (tree verificado integro); inventario stale pre-existente

## RE-ENTREGA 2 tras re-OBJECION C-004 Codex (hash 8346dd1: "TS omite Policy/AST; Py acepta DROP_TABLE/v2/NaN; pack stale")
commit: 57ee451 · Ref C-004 · regla derivada K-024 (paridad semantica bilateral fail-closed)
paths: src/contracts/{policy,policy_dsl,rule_trace}.py + lib/contracts/policy.contract.ts
+ tests/unit/test_policy_contract.py (12 -> 67 tests)
mapeo hallazgo->remedio 1:1: (1) TS ahora declara interfaz Policy (Protocol espejo) +
ComparisonOperator/LogicalOperator/RangeOperator + FeatureRef/Operand + nodos AST
ComparisonCondition/AllAnyCondition/NotCondition/BetweenCondition = misma gramatica que
validate_condition; (2) POLICY_MODES whitelist + __post_init__ lanza en construccion
(DROP_TABLE -> ValueError) y validatePolicyContext TS rechaza igual; (3)
SUPPORTED_TRACE_SCHEMAS=(v1,) valida en constructor Y from_dict; espejo TS; (4)
isfinite en StrategyDecision.target_exposure + defensa en profundidad en parse del DSL
(defaults, output por regla, operandos literales, between estructural) + Number.isFinite
en validateStrategyDecision TS; rango NO declarado en el contrato => solo finitud
(declarado, no inventado); (5) este pack contra hash nuevo; (6) tests de RECHAZO
bilaterales: 4 clases Python fail-closed + 3 clases de paridad TS que exigen que cada
validador codifique el mismo rechazo; rechazos identicos demostrados EN VIVO en ambos
runtimes (salidas literales en reporte del agente, reproducibles con los comandos).
comandos: python -m pytest tests/unit/test_policy_contract.py -q; npx tsc --noEmit (delta)
resultados: 67 passed (re-ejecutado por raiz post-commit); tsc 334 = 334 baseline (0 nuevos)
lanes vivos sobre estos paths: NINGUNO (2026-07-27T23:05:00-05:00)

## RE-ENTREGA 3 tras OBJECION(re-review2) CXD-015
commit: 117e1127758336176e26d67c16561ec028c1d6bf · Ref C-004
paths: src/contracts/{policy,policy_dsl,rule_trace}.py + lib/contracts/policy.contract.ts +
tests/unit/test_policy_contract.py + tests/unit/contracts/policy-contract-parity.test.ts (NUEVO)
mapeo 1:1: (1) bool/string exposure => _strict_exposure isinstance-bool-first, cero float()
coercion; (2) {feature:true}/'feature.' vacio => ValueError bilateral; (3) snapshot Infinity
=> validate_inputs + _snapshot_value exigen real finito, TS validateFeatureSnapshot; (4)
policy_hash=True / sleeve_id=None / as_of=None => require_hash(^sha256:hex{8,64}$) +
require_id + require_iso en construccion; (5) JSON: allow_nan=False + ensure_json_safe
recursivo en decision_components/observed — Inf inyectado post-construccion LANZA; (6) tests
que ejecutan, no inspeccionan: MISMA tabla de 35 casos (ids/payloads/veredictos duplicados
literal + pin) corrida contra validadores Py reales y validadores TS compilados; (7)
from_dict sin trace_schema => rechazo en ambos (semantica unificada).
fail-first: 10 huecos reproducidos ACCEPTED pre-fix (salidas en reporte del agente),
post-fix los 11 checks RAISED.
comandos: python -m pytest tests/unit/test_policy_contract.py -q => 141 passed (re-ejecutado
por raiz post-commit); npx vitest run tests/unit/contracts/policy-contract-parity.test.ts =>
36 passed (re-ejecutado por raiz); npx tsc --noEmit => 0 nuevos; test_contract_mirrors 18 passed.
cambios de comportamiento declarados: as_of ISO obligatorio; policy_hash forma estricta;
evaluate exige context.as_of; fixtures actualizadas en el mismo commit.
lanes vivos sobre estos paths: NINGUNO (2026-07-27T23:40:36-0500 — leases liberados al commitear)
