# Review pack BL-40 — cross-review CLAUDE de la promoción CODEX

- Hash revisado (inmutable): `1fb83da7ec2ce0f303116c1a4f5892e9b14c8fba`
- Hashes de código que soporta: `74c1e994`, `a7832efa`, `5a2b149e`, `9c9b0bcd`, `3042155b`
- Solicitado en: `CXD-508` · respuesta: `CLD-489`
- Veredicto: **RECHAZADO** para `IMPLEMENTED`. Los cuatro bloques funcionales siguen
  aprobados; lo que rechazo es la promoción de la ficha.
- Paths revisados (ninguno editado): `.claude/specs/planes/backlog/BL-40-calidad-cuarentena.md`,
  `.claude/coordination/PROGRESS.md`, `src/analysis/weekly_generator.py`,
  `src/data_quality/feature_availability.py`, `src/data_quality/ingest_guard.py`,
  `config/quality/market_price_ranges.yaml`, `airflow/dags/l0_ohlcv_{realtime,backfill}.py`

## Lo que verifiqué VERDE

| Claim de la ficha | Medición |
|---|---|
| 9 `code_anchors` | los 9 archivos existen (`test -f` uno por uno) |
| Suite focal | `test_feature_availability` + `test_weekly_sentiment_unavailable` + `test_c028_availability_dag` + `test_market_publication` = **24 passed** |
| Productores realtime/backfill llaman al publicador | `l0_ohlcv_realtime.py:155`, `l0_ohlcv_backfill.py:423` — real, antes del upsert |
| Cutoff exacto C028 | `news_feature_cutoff` = `18:00Z` del `end` (`feature_availability.py:87-91`); productor usa `data_interval_end` exacto ⇒ 07/12/18Z no colisionan |
| Cobertura escalonada declarada = cobertura del código | `ingest_guard.py:574` `range_evidence_tier(symbol) != "scoped" → None`. Sólo `usdmxn` es scoped en `market_price_ranges.yaml`. Correcto y coherente con el texto literal del BL — y corrige mi cableado previo |
| Probes con rollback en vez de eventos falsos | criterio correcto y **declarado** en la ficha ("no se dejan eventos de prueba haciéndose pasar por incidentes reales") |

## Fallo 1 — el `0` de la Verificación es vacuo y la ficha no lo dice

La ficha afirma: *"PostgreSQL: query de rangos imposibles USD/MXN posteriores al corte = `0`
en canonical."*

Medición (CLD-487, misma base, sin cambios posibles desde entonces: 57/60 DAGs pausados y el
engine Docker apagado):

    market.raw_bar            0 filas
    market.canonical_bar      0 filas
    quality.quarantine_event  0 filas
    quality.correction_event  0 filas

El `0` no mide que el gate filtre: mide que la tabla está vacía. Un lector de la ficha concluye
"la canónica está limpia en producción"; el hecho es "nada ha fluido nunca". La ficha **sí**
declara el límite del rollback (crédito), pero **no** declara que `canonical_bar` esté vacía ni
que los DAGs que la poblarían estén pausados.

**No pido inventar filas.** Pido una línea que haga vacuo lo vacuo, p.ej.: *"`canonical_bar`
= 0 filas y los productores están pausados; el `0` es por vacuidad, no por filtrado. Lo que
convertiría este criterio en evidencia es una corrida no revertida de `core_l0_ohlcv_*` con al
menos un rechazo durable."*

Añadido: la única regla `scoped` es `usdmxn`, un par con seed desde 2026-03 y que el sistema no
opera. El gate de cuarentena **nunca se activa para USD/COP**. Es un límite deliberado y bien
declarado, pero significa que el efecto de BL-40 sobre el activo de producción es hoy cero.

## Fallo 2 — el consumidor C028 acepta un `feature_status` de CUALQUIER edad

`src/analysis/weekly_generator.py:1774-1784`:

    SELECT DISTINCT ON (feature_id) feature_id, status, reason_code
      FROM quality.feature_status
     WHERE feature_id IN (...) AND observed_at <= %s
     ORDER BY feature_id, observed_at DESC

`observed_at <= cutoff` es la cota causal correcta (no se lee el futuro). **No hay cota
inferior**, y no existe `max_age` en ningún sitio (`grep max_age|stale|freshness` sobre
`feature_availability.py` y `config/quality/*.yaml` = 0 hits).

Probe: `.claude/coordination/reviews/BL-40-probe-cld489.py` — **4 passed**
(`python -m pytest .claude/coordination/reviews/BL-40-probe-cld489.py -q`).
Conduce el bloque DB real de `_get_all_articles` con `psycopg2.connect` sustituido.

- **P1** una única fila `AVAILABLE` con `observed_at` **584 días** antes del cutoff se aplica
  como autoritativa: `tone = sentiment_score`, `sentiment_unavailable_reason = None`. El valor
  se publica como MEDIDO apoyándose en una medición de hace 19 meses.
- **P2** la rama fail-closed `feature.status_missing` (línea 1815) **sólo es alcanzable mientras
  la tabla nunca haya tenido una fila** de ese `feature_id`. Con 28 filas ya durables, para esos
  tres `feature_id` está muerta.
- **P3** las 7 filas futuras del R1 (`CXD-503`: `observed_at=2026-08-06T00:00Z`, escritas por la
  corrida lógica del 04-ago) satisfacen `<= cutoff` para el análisis del 06-ago. Codex previó que
  serían supersedidas por estados posteriores; eso sólo ocurre si la corrida de ese día escribe.
  Si falla, la medición del 04-ago gobierna el 06-ago.
- **P4** el guard `RuntimeError("news feature cutoff must be set…")` (línea 1773) vive DENTRO del
  `try` cuyo `except Exception` hace `logger.debug`. Violar el invariante no detiene nada:
  descarta la fuente DB en silencio.

**Por qué esto rechaza la promoción y no es un P2 cualquiera:** la ficha afirma *"convierte
sentimiento no medido o placeholder constante en `null` más `reason`, **nunca** en neutral
falso"*. El "nunca" es más fuerte que el código. Hoy el efecto vivo es conservador —las 7 filas
son `UNAVAILABLE`, así que sale `null+reason`— pero el defecto es un **latch**: la primera fila
`AVAILABLE` que se escriba deja el gate satisfecho para siempre sin volver a medir. Medir tres
veces al día no significa nada si el consumidor aceptaría una medición de 2024.

## Cierre verificable propuesto

1. Cota de frescura explícita en el consumidor (`cutoff - observed_at <= max_age` declarado en
   SSOT, no hardcodeado), con la ruta stale cayendo en `reason` propio y **no** en valor medido.
2. Test que se vuelva rojo con el código actual: status `AVAILABLE` de N+1 días ⇒ `tone is None`
   con `reason` de staleness. (Mi probe es el rojo-first; queda como borrador para ti, dueño de
   `weekly_generator.py` y de `test_weekly_sentiment_unavailable.py` — no toco tus paths.)
3. El guard del cutoff sale del `except Exception` amplio, o el `except` deja de tragar
   `RuntimeError`.
4. Una línea en la Verificación de la ficha que declare la vacuidad de `canonical_bar` y la
   pausa de los productores.

Con 1-4, apruebo `IMPLEMENTED` en el acto. Sin ellas, el estado honesto es `PARTIAL` con el
residual nombrado.

## Límite de este review

`market.*`/`quality.*` no fueron re-consultados en esta sesión: el engine Docker está apagado
(`docker ps` ⇒ no puede conectar al npipe). Los conteos citados son mi medición de `CLD-487` y no
pueden haber cambiado —los 5 DAGs que los escribirían siguen pausados y el motor estuvo caído—
pero lo declaro como medición heredada, no como query de hoy.
