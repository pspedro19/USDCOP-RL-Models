# BL-22 — paquete inmutable de semántica contable

- Estado solicitado: `APROBADO_PARCIAL`; no incrementar marcador.
- Corte: `67a38a99e1d957a64d74ea8fbdc9609680cb1f3e`
- Cambio de producción: ninguno. La migración 075 ya tenía la expresión correcta.

## Paths

| Path | Blob Git | SHA-256 |
|---|---|---|
| `tests/unit/test_codex_safety_contracts.py` | `d6695b902ba575f210395c85c5bc24c734520fbc` | `BF91900D79AACD5B065C82B0C95B4B4E70E59115DD970D5795B66245FA2C6BF7` |
| `.claude/specs/planes/backlog/BL-22-fact-position-pnl.md` | `d88a131a3d435993ab505c5755e14eaa07c50464` | `E6485C3E0BB41ACC9897C1477DE1DE6B509436E4C0890EA6B6BB1EBAFB9431DF` |

Oráculo de producción sin diff:

- `database/migrations/075_fact_position_pnl.sql`
- blob `b8af278e99b9c186e2060a6514d50a62f3e37e21`
- SHA-256 `F02A51D90BC828DBF9F347FD518B67525B88FAE29E81AAA6C924975B9FE8144F`

## Evidencia

El test extrae la expresión completa posterior a
`) AS calculated_residual,` y la ejecuta sobre SQLite. Con residual calculado
`-5`, exige:

- reportado `-3` → error `2`;
- reportado `3` → error `8`.

Esto distingue sin ambigüedad:

```text
ABS(calculated - reported)    # producción, correcto
ABS(calculated) - reported    # receta de revisión, incorrecto
```

Archive limpio:

```powershell
python -m pytest tests/unit/test_codex_safety_contracts.py -k pnl_identity -q
# 2 passed, 17 deselected
```

Mutaciones:

1. mover el cierre de `ABS` antes de restar el residual:
   `1 failed / 1 passed`, mensaje `8 != 2`;
2. prefijar la expresión con `0 *`:
   `1 failed / 1 passed`, mensaje `0 != 2`.

La segunda mutación sobrevivió a una primera versión local del extractor porque
éste empezaba dentro de `ABS`; el extractor final captura la expresión completa
y el mutante fue reejecutado en rojo. La migración se restauró sin diff.

## Límite

`MetricEngine` todavía devuelve timing ratio sin IC. El script one-off sí
calcula IC por bloques, pero declara `persisted: false`. El plan Fabric tampoco
está aplicado en PostgreSQL vivo. Por tanto, sólo se pide aprobar el candado
contable y corregir el diagnóstico del paréntesis; BL-22 sigue PARTIAL.
