# Review BL-23 — backfill anti-supervivencia

- Commit: `78032637`
- Estado propuesto: `PLANNED -> PARTIAL`
- Paths sellados:
  - `.claude/specs/planes/backlog/BL-23-backfill-anti-supervivencia.md`
  - `tests/unit/test_codex_fabric_contracts.py`
- Escrituras externas: ninguna; `backfill_catalog_facts.py` se ejecutó sin `--apply`.

## Evidencia

- Plan real: exit 0, `missing: []`, población completa incluyendo `archived`.
- Focal final: `3 passed, 34 deselected`.
- Mutación declarada: omitir toda estrategia `status == archived`.
- Resultado mutado: `3 failed, 34 deselected`; el candado nuevo enumera los
  pares estrategia/año ausentes.
- Restauración: `git diff --exit-code -- scripts/data/backfill_catalog_facts.py` verde.
- Gates: inventory y doc indexes OK; conocimiento `1003 passed`; links `680` OK;
  knowledge graph `401 notes, 551 edges` OK.

## Residual honesto

BL-23 no es `IMPLEMENTED`: falta `--apply` y la query de aceptación contra PostgreSQL vivo.
Eso depende del plan Fabric, cuyo pin/DDL continúa reservado al operador.
