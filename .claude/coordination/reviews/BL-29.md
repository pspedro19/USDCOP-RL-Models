# BL-29 R2 — paquete inmutable para revisión cruzada

- Estado solicitado: `APROBADO_PARCIAL`; no incrementar el marcador.
- Corte: `b162e4a0bb16ed16d8187654981e0dd6d46eeb78`
- Padre: `235b9a5cc077da8fa5f28436c4857bbdeb67fda9`
- Incidente de autoría: los cinco paths BL-29 estaban staged bajo lease Codex,
  pero el commit concurrente CLD-253 capturó el índice compartido. No se
  reescribió ni duplicó el commit. El alcance se revisa por los blobs y paths
  listados abajo.

## Paths y blobs

| Path | Blob Git | SHA-256 |
|---|---|---|
| `scripts/analysis/qlab.py` | `4b551f746d836e5a3a34d9c89ef37c9e5bb4a7d5` | `4C1F00A31146352D45C4CC54C6548631882E6C7AEB7E949518033A1FAE8C07DB` |
| `src/research/point_in_time.py` | `6c73941ad53aa36202877c22aecae579fdea41f3` | `247F5ABAA9B0B2FB9E6619F13F837447689B020D1493B566D2E73C11923DB634` |
| `src/research/qlab.py` | `db47a00b187774b0206e1c38b68a1616b2495e8f` | `D541AA3CC23724DAC9EB5D0A3143A968FE3B4FEA39465C47AC7E8ECEDC9DBC77` |
| `tests/unit/test_qlab_point_in_time.py` | `2648e7d59f5fd28f4840184aca6cfe8a1cba5462` | `5BDD48BEF5AA9BF3B95A919788BCD74408EE846B8790A9EE795F14CB9582FA0B` |
| `.claude/specs/planes/backlog/BL-29-qlab-cli-cutoff-lectura.md` | `2e812957d5035001c2078312298426fd55d39829` | `28524B4F10D6B66438C11CE3C29308FD06405C1E402BAFA1B860F87AEAA87F68` |

## Rojo, verde y restauración

Rojo R2 antes de implementación:

```text
tests/unit/test_qlab_point_in_time.py: 7 failed, 2 passed
```

Verde contra archive limpio del commit:

```powershell
python -m pytest tests/unit/test_qlab_point_in_time.py `
  tests/unit/test_codex_fabric_contracts.py::test_qlab_idempotency_rejects_same_trial_id_with_changed_result `
  tests/unit/test_codex_fabric_contracts.py::test_family_projection_repairs_after_ledger_only_retry `
  -q --basetemp=.pytest-bl29-clean
```

Resultado: `13 passed in 0.27s`.

Archive local de revisión:
`.claude/coordination/tmp/bl29-r2-b162e4a0-clean-20260729-0227/`.

Mutaciones independientes, siempre restauradas antes de la siguiente:

1. `trial_id` de longitud abierta: `1 failed / 1 passed`.
2. `additional_where` crudo reintroducido: `1 failed`.
3. validación post-read async retirada: `1 failed`.
4. hash de bytes CRLF sin normalización: `1 failed`.
5. campos auditables omitidos al cobrar: `1 failed`.
6. fecha resuelta como fin-de-día UTC fijo: `2 failed`.
7. overrides `--data-hash` y `--available-at-field` reabiertos: `1 failed`.
8. identidad familia↔activo retirada: `1 failed`.

Los cuatro SHA-256 de producción/test tras restaurar coinciden con la tabla.

## Qué prueba y qué no

Prueba que el CLI de fichero:

- valida `^(FT|AT)-\d{4}$` antes de tocar el ledger append-only;
- no ofrece un escape SQL por predicado crudo;
- guarda sync y async después de materializar;
- deriva evidencia LF/CRLF canónica sin overrides;
- persiste campo causal, número de filas y máximo `available_at`;
- resuelve fechas con la zona horaria del activo declarado por la familia.

No prueba ni afirma un adaptador DB/BL-19 conectado en producción, ejecución del
modelo/estadístico por el propio CLI, PostgreSQL real, CI amplio ni Playwright.
Por eso el estado solicitado sigue siendo `APROBADO_PARCIAL`.
