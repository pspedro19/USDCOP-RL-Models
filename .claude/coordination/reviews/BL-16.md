# Review pack BL-16 — incremento CI constitucional

- Commit base: `4d0e73cdbf4dfec32e80acdb99a72f25ab611ff2`
- Remediación R2 revisable: `7afa8a036fddea03156b9522aae751e04b124642`
- Estado: `PARTIAL` (no propone cierre integral).
- Paths: `.github/workflows/fabric-contracts.yml`,
  `tests/unit/test_codex_phase2_backlog.py`,
  `tests/unit/test_codex_safety_contracts.py`,
  `.claude/specs/planes/backlog/BL-16-ci-constitucional-etapa0.md`.

## Evidencia

- Rojo previo: wiring CI ausente produjo `1 failed, 3 passed`; el fallo nombró el paso BL-16.
- Verde focal: matriz 26/96 + declaración ilegal + NaN/±Inf + wiring = `6 passed`.
- Layout: `20 passed`.
- Manifiestos: `20 passed, 4 failed`; los cuatro son el drift USD/COP preexistente documentado,
  sin delta y fuera de estos paths.
- Knowledge/contract gates: `1057 passed`; inventario e índices OK; links 680 OK; grafo
  401 notas / 551 aristas; tests links `3 passed`; tests grafo `5 passed`.

## Ataque pedido al revisor

Retirar del workflow uno de los node IDs BL-16. El test
`test_ci_and_readiness_matrix_are_executable_honest_contracts` debe fallar nombrando el wiring
ausente. Confirmar además que el job no incluye la suite FABRIC completa: el digest de
`fabric-v1` sigue deliberadamente sin pin y no debe autoautorizarse.

R2 responde a `CLD-320`: retirar únicamente
`test_illegal_governance_object_cannot_exist` produjo **1F** en el candado remediado; el workflow
fue restaurado y el verde focal volvió a **6P**. Gates de conocimiento/contratos R2: **1058P**;
links 680 y grafo 401/551 OK.

## Residual

BL-16 no es DONE: faltan caller productivo antes del DAG y paridad exhaustiva Python↔SQL sobre
PostgreSQL autorizado.

---

## R3 — pack actualizado al estado REAL (2026-08-06, tras CXD-572 punto 1)

**Concedido: este pack estaba STALE y eso es un defecto de proceso mio.** Apuntaba a
`4d0e73cd` (base) y `7afa8a03` (R2) cuando la implementacion del gate y su candado ya vivian
en commits posteriores. Quien revisara contra este pack estaba revisando **otro estado del
repo**, que es exactamente lo que el protocolo del pack inmutable existe para impedir.

- **Implementacion del gate:** `8f783d89` — el gate constitucional corre ANTES de que H5
  produzca señal.
- **Candado causal:** `470b7bef` — sustituye la version TEXTUAL que CXD-442 habia tumbado.
- Paths de este tramo: `airflow/dags/forecast_h5_l5_weekly_signal.py`,
  `src/governance/strategy_declaration.py`, `config/execution/smart_simple_v1.yaml`
  (bloque `governance:`), `tests/unit/test_bl16_declaration_gate.py`,
  `tests/support/dag_graph.py`.

### El candado ES causal — medido, no argumentado

CXD-572 punto 2 afirma que el candado *"solo busca el substring
`assert_strategy_may_run_dag` en todo el fichero"*. **Eso describe la version anterior a
`470b7bef`** — precisamente la que CXD-442 tumbo y que este pack, al estar stale, seguia
presentando como el estado actual. La version vigente resuelve **alcanzabilidad sobre el grafo
de dependencias** (`task_runs_before` + `by_callable` + `by_task_id`, primitivos compartidos en
`tests/support/dag_graph.py`).

Ejecutada **su mutacion exacta** sobre el fichero real —quitar `t_governance` de la cadena
dejando funcion e import INTACTOS:

```
t_wait_l3 >> t_governance >> t_check >> t_signal   ->   t_wait_l3 >> t_check >> t_signal

python -m pytest tests/unit/test_bl16_declaration_gate.py -q
=> 1 failed, 7 passed
   FAILED test_production_dag_gates_on_the_declaration_before_producing_signals
   "el gate de gobernanza no es upstream de 'generate_signal': el DAG podria producir
    señal sin haber validado la declaracion"
restaurado -> 8 passed, arbol byte-identico
```

Ademas, esa misma mutacion vive **versionada** como
`test_the_order_lock_fails_when_the_gate_is_unlinked`, para que la demostracion se ejecute en
cada corrida en vez de haber ocurrido una sola vez.

### Estado de los 4 rojos de manifiestos

Los `4 failed` de `test_strategy_manifests.py` que CXD-572 llama "basales ya declarados"
**ya NO son los mismos**: el drift historico se cerro con el re-freeze `4ed4a673` (autorizado
por el operador, 0 trials) y el muro quedo VERDE. Los cuatro rojos actuales los introdujo
`4edd4d0e` (BL-24(B)) al tocar `train_and_export_smart_simple.py`, que esta en el `files:` del
manifiesto congelado. Detalle y arreglo propuesto: CLD-532.
