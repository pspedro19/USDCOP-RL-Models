# BL-07 — paquete de revisión inmutable

- owner: CODEX
- implementation_commit: `d0427d670ff50f8179a29de2b01032e0324202e7`
- contract: `C-EXEMPT` — JSON diagnóstico one-off, no persistido ni consumido como
  contrato compartido
- estado solicitado: `PARA_REVIEW`

## Rutas del commit

- `.claude/specs/planes/backlog/BL-07-timing-ratio-etapa05.md`
- `scripts/analysis/profitability_adapters.py`
- `scripts/analysis/profitability_evidence.py`
- `scripts/analysis/profitability_types.py`
- `scripts/analysis/timing_ratio_oneoff.py`
- `tests/unit/test_timing_ratio_oneoff.py`

## Alcance verificable

- Reporta las cuatro campeonas en orden fijo.
- USD/COP v11 queda `UNAVAILABLE`: el adapter no contiene retorno subyacente y no se
  fabrica un proxy.
- BTC B1 tiene `n_trades=1`: por `N<20` conserva conteos/PnL y suprime ratio,
  covarianza e IC.
- Gold/SPX publican atribución finita con bootstrap circular pareado 20/5000/42.
- `gold_dynamic_exit` se añade únicamente como control manual; su IC contiene cero.
- Salida estrictamente diagnóstica, 0 trials, no persistencia y JSON sin NaN/Infinity.

## TDD y self-red-team

- Rojo inicial de implementación: 3 fallos/1 pass por estados ausentes.
- Self-red-team encontró publicación de IC para BTC N=1.
- Test fail-first específico: `1 failed, 6 passed` antes del guard constitucional.
- Después del fix: `7 passed`.

## Verificación final

1. `python -m pytest tests/unit/test_timing_ratio_oneoff.py -q`
   - `7 passed`; warning preexistente por opción `asyncio_mode`.
2. `python scripts/analysis/timing_ratio_oneoff.py --include-gold-control`
   - exit 0 en 17.466 s;
   - cuatro campeonas + control;
   - v11 `UNAVAILABLE`;
   - BTC `INSUFFICIENT_TRADES`, N=1, sin ratio/IC;
   - Gold control ratio 0.0206378474, IC95
     `[-0.0076845889, 0.0486331161]` (contiene 0);
   - `diagnostic_only=true`, `promotion_eligible=false`, `trials_charged=0`,
     `persisted=false`.
3. `python -m pytest tests/regression/test_knowledge_frontmatter.py -q`
   - 47 failed / 751 passed / 74 skipped: mismos 47 fallos de BASELINE; delta 0.
4. `python -m pytest tests/regression/test_strategy_manifests.py -q`
   - `9 passed` usando basetemp escribible; intentos previos tuvieron únicamente
     `PermissionError` de tmp del sandbox, no fallo funcional.
5. `python -m pytest tests/regression/test_scripts_layout.py -q`
   - `20 passed`.
6. Import ligero:
   - `profitability_adapters` carga sin `psycopg2`;
   - identidad `profitability_adapters.Sleeve is profitability_types.Sleeve`;
   - fórmula neta preservada.

## Criterio para Claude

Revisar exclusivamente el hash completo indicado. `APROBADO` requiere reproducir
las verificaciones sin ratio/IC para N<20, sin proxy numérico COP, JSON estricto y
runtime menor de cinco minutos. El MD permanece `PLANNED` hasta cross-review; CODEX
lo cambia a `IMPLEMENTED` solo después del veredicto.
