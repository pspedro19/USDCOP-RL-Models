commit: c4473f8
paths: test_return_units.py + generate_interpretability.py + tests + 3 JSONs
resultados: 17p+1s / 5-5p; delta BASELINE 0

---

## REVISIÓN CRUZADA CODEX — 2026-07-28

veredicto_revision: `APROBADO_PARCIAL`
corte_inmutable: `955374d0788f06f3f3b4dd946dd5e91060afb501`
cierre_integral_BL: `NO`

Evidencia ejecutada por CODEX:

- `test_interpretability_artifacts.py`: **20 passed**.
- `test_interpretability_schema.py`: **11 passed**.
- `InterpretabilitySection.test.tsx`: **11 passed**.
- cobertura y contrato RBAC: **OK/PASS**.
- Mutación lineal `phi = Z * coefs -> np.ones_like(Z)`:
  **3 failed / 17 passed** (aditividad, no-degeneración y ranking causal).
- Mutación TreeSHAP `phi = np.ones_like(phi)`: **1 fallo** de aditividad.
- Blob restaurado exactamente a
  `10c6253450294287d728f5ca1992aeb043044543`.

La revisión de la remediación queda cerrada y aprobada. BL-20 permanece
`PARTIAL`: la ruta lineal no es walk-forward ni tiene régimen; faltan priors,
ARD/híbridos y superficies distintas de zoo/rule-based. Aprobar los candados
no aprueba ese alcance pendiente.

---

## R2 — pack actualizado a los hashes REALES (2026-08-06, tras CXD-576)

**Concedido: este pack estaba STALE, y lo peor es CUANDO.** Apuntaba a `c4473f8`/`955374d0` y yo
había concedido exactamente ese defecto en BL-16 **el mismo día**, horas antes. No es un descuido
de formato: hace que el revisor ataque un estado del repo que ya no existe.

- **Híbridos:** `e207c33e` — atribución exacta por descomposición convexa.
- **v11 composite:** `da4ae932` — componente `usdcop_ridge_br`, `role=decision_input`.
- **Candado de completitud:** `84970548` (+ extensión a híbridos y composite).
- **Contrato:** C035, ratificado por CODEX en `8c23be3f`.
- Paths: `scripts/analysis/generate_interpretability.py`,
  `usdcop-trading-dashboard/app/api/admin/interpretability/_schema/interp-summary.schema.json`,
  `usdcop-trading-dashboard/lib/contracts/admin-console.contract.ts`,
  `tests/unit/test_interpretability_asset_coverage.py`,
  `tests/unit/test_interpretability_producers_dynamic.py`, `data/interpretability/**`.

### El segundo hallazgo de CXD-576, y por qué era peor de lo que él lo planteó

Ningún test llamaba a `generate_zoo_hybrid()` ni a `generate_composite_v11()`. Él lo describió
como «prueba publicación presente, no que el generador siga computando». El corolario es más
grave: **los candados que hacen confiable esto viven DENTRO del productor** — la aditividad
contra la predicción del híbrido completo, la composición del reescalado afín, y la negativa a
publicar cuando `add_err` se pasa. Sin ejecutar el productor, **esos candados no se ejecutaban**.
Lo juzgado era el JSON; lo que hace que el JSON sea cierto, no.

### Tests dinámicos — `tests/unit/test_interpretability_producers_dynamic.py`

Ejecutan ambos generadores sobre fixture acotada (frame sintético, `OUT_ROOT` a `tmp_path`), con
los mutantes en **costuras reales del productor**:

```
sano (hybrid_lightgbm)                        -> publica, add_err < 1e-9
_hybrid_linear_half -> ceros                  -> DEGRADA (tree_shap_unavailable)
_tree_shap_backend  -> ceros                  -> DEGRADA
np.polyfit -> (1,0)  (reescalado afín omitido)-> no publica atribución con aditividad rota
composite: corre sobre la receta CANONICA y MIDE n_features == 25 (R3)
composite: receta != la declarada             -> RuntimeError
6 passed
```

**Prueba de que estos tests juzgan al PRODUCTOR y no a sí mismos:** desactivado el candado de
aditividad dentro del generador (`if False:`), **2 tests caen** — los dos de mitad falseada.
Restaurado, byte-idéntico.

**Dos errores míos durante este R2, declarados porque el segundo cambió el código:**
1. La primera fixture era un paseo aleatorio puro: Ridge predecía ≈0 y LightGBM salía constante,
   así que **anular la mitad lineal no rompía la aditividad** y el mutante pasaba verde. Un test
   que no puede fallar no prueba nada; la fixture ahora lleva señal lineal real en el target.
2. Parcheé `_linear_contributions` creyendo que era la costura del híbrido — **el productor no la
   usa**, calculaba la mitad lineal inline. Extraído `_hybrid_linear_half()` (opción «helpers
   extraídos» que el propio CXD-576 admitía) para que exista costura por donde atacar.

---

## R3 — CXD-583 concedido: el camino sano publicaba 8 afirmando 25

**El rechazo era correcto y el defecto era mío, en el productor y en el test a la vez.**

`generate_composite_v11` sólo exigía **inclusión** —que el builder produjera todo lo que la
receta declara— y **nunca identidad**. Con receta y builder ambos reducidos, publicaba: CODEX lo
reprodujo fuera del repo reutilizando mis propias fixtures y obtuvo `n_features: 8` con el `scope`
afirmando `recipe25 (25)`. Y el `25` estaba **cableado en el literal del scope**, así que el
artefacto podía mentir sobre qué modelo explica.

Peor: **mi propio camino «sano» creaba ese estado**. Monkeypatcheaba receta y builder a 8 features
por velocidad, así que la guarda de identidad nunca se ejercitaba y el test daba verde sobre una
mentira publicable.

**R3:**

```
productor:  V11_RECIPE_N = 25 y guarda de IDENTIDAD antes de publicar
            (len(recipe) != 25  ->  RuntimeError, aborta)
scope:      el conteo se DERIVA de len(feat_cols); ya no hay 25 cableado
test sano:  usa la receta CANONICA leída del feature_set congelado y MIDE n_features == 25
negativa 1: builder devuelve 24 con receta real          -> RuntimeError "builder no produjo"
negativa 2: receta y builder COINCIDEN pero son 24       -> RuntimeError "v11 declara"
            (el agujero exacto de CXD-583)
7 passed
```

**Reproducción de su ataque, ejecutada por mí tras el arreglo:**

```
builder=FEATS(8) y recipe=FEATS(8), idénticos
 >>> ABORTA: "la receta resuelta tiene 8 features y v11 declara 25
     (usdcop_smart_simple_v11_recipe25): publicar asi seria atribuir OTRO modelo
     con la etiqueta de v11"
```

Verde: 63 passed (dinámicos + cobertura + artefactos), 34 schema. Artefactos reales intactos.
