# Revisión cruzada BL-36 — Matriz de verdad DB

fecha: `2026-07-29T00:49:00-05:00`
veredicto_revision: `RECHAZADO`
corte_inmutable: `2a608feb`
cierre_integral_BL: `NO`

## Evidencia causal

- Prístino: **8 passed**.
- Mutación declarada de la fila `BI fact_*`,
  `DEPRECATED / 7 ficheros` a `AUTORITATIVA (escritor único) / 0 ficheros`:
  **2 failed / 6 passed**.
- Mutación separada que conserva `DEPRECATED` y cambia solo `7 -> 0`:
  **2 failed / 6 passed**. Caen el conteo medido y la divulgación de lectores
  vivos.
- Restauración exacta de la matriz:
  SHA256 `A16304947678D550C306DC1D5F2DC5E57C3706C6EB1831A204B9216F3CB37E7F`,
  blob `2e677e028770c0d7e530eae62cd508a051898371`; prístino restaurado
  **8/8**.

## Bloqueante del paquete

El oráculo `.claude/generated/db-inventory.json` no es reproducible desde el
commit que lo publica:

- `python scripts/diagnostics/db_inventory_matrix.py --check` en checkout limpio
  devuelve exit 1: inventario stale.
- El inventario publicado declara **242 objetos**; regenerarlo desde los inputs
  de código y DDL del mismo commit produce **253**.
- Aparecen 14 objetos, desaparecen 3 y cambian writers/readers de 11 objetos
  existentes.
- El JSON publicado cita cuatro módulos ausentes del commit y sin historia Git:
  `airflow/dags/l0_usdcop_forward_macro.py`,
  `scripts/pipeline/generate_usdcop_h1_daily_shadow_v1.py`,
  `scripts/validation/audit_partial_ohlcv_bars.py` y
  `src/data/usdcop_forward_macro.py`.

Los ocho tests prueban coherencia entre la prosa y un JSON stale, no entre la
prosa y el repositorio que el JSON afirma medir. Para re-review se requiere
regenerar y commitear el inventario desde checkout limpio, ejecutar `--check`
como gate, actualizar las afirmaciones afectadas y repetir la mutación.
