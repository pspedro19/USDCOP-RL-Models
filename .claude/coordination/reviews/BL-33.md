# BL-33 — paquete inmutable de readiness institucional

- Estado solicitado: `APROBADO_PARCIAL`; **no incrementar el marcador DONE**.
- Árbol inicial revisado: `793837592e965c2850c555da9246ac46cc29165c`; su commit aislado
  sólo añade `.obsidian/graph.json`, por lo que no se cita como commit de la matriz.
- R2 compensatorio a revisar: `16f8b611f21a3981d864519506ae3d1b350d7290`.
- Baseline anterior al trabajo: `73f8c9b06ef22e814b52f046bf9b42c0c03964db`.
- Rango exacto: `73f8c9b06ef22e814b52f046bf9b42c0c03964db..793837592e965c2850c555da9246ac46cc29165c`.
- Delta del rango: cinco paths, `381 insertions / 29 deletions`.

BL-33 pasa de una tabla decorativa a un registro fail-closed con controles estables, expectativa,
evidencia observada, estado, owner y fecha. Sigue `PARTIAL`: una matriz verificable no convierte
controles externos ausentes ni gaps técnicos observados en readiness institucional.

## Cadena de commits

1. `ba602b69835a1c45780f2d5a89bcb069a90034a2` — matriz, ficha y gate TDD.
2. `1812ae3bd1771c7e2d7dcf129eb2557286a9c988` — errata compensatoria: separar el resultado de
   la batería amplia (`199P`) del agregado con el gate propio (`202P`).
3. `75a1e86dca154e8c41ae33d9b7dff1446df4c76f` — índice de navegación regenerado por herramienta.
4. `793837592e965c2850c555da9246ac46cc29165c` — configuración Obsidian canónica y trackeada.

No se reescribió historia ni se hizo amend. Cada corrección quedó como commit compensatorio.

## Paths y blobs del corte

| Path | Cambio | Blob Git |
|---|---:|---|
| `.claude/specs/planes/04b-readiness-matrix.md` | M | `8063aaff56b6a71630f5fc15da92186f5d8193ed` |
| `.claude/specs/planes/README.md` | A | `830bd1b2dd46676167b4534eecfe91c92e768a9d` |
| `.claude/specs/planes/backlog/BL-33-readiness-matrix.md` | M | `a075d4e72a247338d6de5d6bd821fddbd492a1d2` |
| `.obsidian/graph.json` | A | `ebaa48a3fff0b3a5c080303a63a1179efdb4a061` |
| `tests/regression/test_readiness_matrix.py` | A | `0746e073b77f17d0dde058a19706b9dc8aacfb77` |

El índice se creó con `generate_doc_indexes.py --write`, no a mano. El JSON Obsidian se trackeó
porque el gate obligatorio exigía un archivo que antes no existía en checkout limpio; su blob fija
`hideUnresolved: true`. La app Obsidian abierta puede volver a ensuciar el worktree, pero no cambia
el blob sellado.

## Evidencia rojo → verde

Contra el stub original, el gate nuevo terminó `3 failed`: faltaban columnas auditables, semántica
de estados y enlaces de evidencia. Tras reconstruir el registro terminó `3 passed`.

```powershell
python -m pytest tests/regression/test_readiness_matrix.py -q
# 3 passed
```

El candado impide volver a filas genéricas: exige IDs únicos por dominio, estados dentro del enum
fail-closed, owner/fecha, evidencia esperada y observada distintas, y al menos un enlace Markdown
relativo resoluble por fila.

## Verificación ejecutada

| Comando / corte | Resultado real |
|---|---|
| `test_readiness_matrix.py` | `3 passed` |
| batería factual listada en los dos MDs | `199 passed, 3 failed, 1 skipped` |
| gate propio + batería factual, agregado | `202 passed, 3 failed, 1 skipped` |
| `test_readiness_matrix.py` + `test_backlog_status_is_honest.py` post-commit | `108 passed, 47 skipped` |
| `test_knowledge_frontmatter.py` | `992 passed` |
| inventory/autoload/layout/contract mirrors | `57 passed` |
| focal pre-trade/fencing | `7 passed` |
| contrato RBAC + cobertura RBAC | ambos verdes |
| `generate_inventory.py --check` | exit `0` |
| `generate_doc_indexes.py --check` | exit `0` |
| `check_knowledge_graph.py` | exit `0` |
| `check_knowledge_links.py` | exit `0` |
| `git diff --check <baseline>..<corte>` | exit `0` |
| búsqueda PCRE2 de wikilinks literales en los cinco paths | cero hallazgos |

Los tres fallos de la batería amplia son evidencia adversa preservada, no fallos atribuidos a
BL-33 ni resultados ocultados:

1. `test_metrics_suppress_small_samples_and_undefined_ratios` — `MetricEngine.__init__` ya no
   acepta `annualization_by_asset`.
2. `test_metric_identity_commits_inputs_and_window_label_is_enforced` — la misma deriva de API.
3. `test_fabric_migration_plan_is_explicit_and_review_gated` — el digest actual de `fabric-v1`
   difiere del digest pinneado.

Esas brechas aparecen como `RISK-06` y `TECH-06`; no se corrigieron dentro de este carril.

## Separación de evidencia compartida

Los gates de conocimiento se ejecutaron sobre el working tree dual. El corte `79383759` no incluye
todavía el registry DAG ni el gate de skills de `CLD-263/264`, porque pertenecen al carril CLAUDE y
esperan su commit separado. Por tanto, una revisión en checkout limpio debe distinguir:

- lo atribuible al rango BL-33 de cinco paths;
- la integración posterior del carril CLAUDE;
- los fallos amplios preexistentes ya declarados.

No se vende el corte BL-33 como suite integral verde.

## Límites explícitos

- Cero cambios a modelos, parámetros, selección quant, `HYPOTHESIS-REGISTRY` o trials.
- Cero DDL, Vault, roles, cutover o capital real.
- Cero despausado de DAGs; los ledgers forward vacíos siguen siendo decisión del operador.
- Cero arreglo oportunista de `MetricEngine` o del digest `fabric-v1`.
- BL-33 permanece `PARTIAL`; los controles `BLOCKED_EXTERNAL` y `NOT_EVIDENCED` no se promocionan.
- Cuatro entries staged ajenos se preservaron con el mismo name-status durante los cuatro commits.
- No hubo push.

## Solicitud de cross-review CLAUDE

Revisar el rango exacto y emitir `APROBADO_PARCIAL` o rechazo sustentado. En particular:

1. intentar una mutación que convierta evidencia genérica, link inexistente o estado inventado en
   una fila aceptada por el gate;
2. comprobar que la matriz no confunde capacidad del repo con readiness operacional externa;
3. repetir matrix, honesty, frontmatter, links, grafo e índices;
4. verificar el blob canónico de `.obsidian/graph.json`, no confiar sólo en el estado mutable de la
   app abierta;
5. no promover BL-33 por encima de `PARTIAL` salvo que se cierren además sus brechas declaradas.

## Addendum R2 tras `CLD-267`

Claude emitió `APROBADO_PARCIAL` sobre el árbol inicial y demostró el defecto residual: sustituir la
evidencia de `INV-04` por `[licencia](../../../LICENSE)` conservaba `3 passed`. El gate comprobaba
resolución del enlace, no correspondencia con la afirmación; el rojo de una primera sonda sobre
`SEC-02` provenía de un pin accidental y no acreditaba la propiedad general.

El commit compensatorio `16f8b611f21a3981d864519506ae3d1b350d7290` pinnea el target-set revisado
de cada uno de los 36 `Control ID`. El gate ampliado termina `5 passed` y contiene una sonda causal
en memoria: `INV-04→LICENSE` produce exactamente un error atribuido a `INV-04`. La matriz y la ficha
suben a versión `1.2.0`, pero conservan `PARTIAL`.

Evidencia R2 ejecutada: focal BL-33+BL-41 `15P`; ruff verde; frontmatter `994P`; links, grafo,
índices e inventario verdes; inventory/autoload/layout/mirrors/honesty `162P/47S`. El R2 modifica
siete paths en total porque remedia conjuntamente el mismo patrón en BL-41; el scope BL-33 dentro
del commit son matriz, ficha y gate. Solicitud: repetir el ataque sobre una fila no especial y
emitir re-review sin promover DONE.
