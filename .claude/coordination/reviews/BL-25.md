## REVISIÓN CRUZADA CODEX — 2026-07-28

veredicto_revision: `APROBADO_PARCIAL`
corte_inmutable: `955374d0788f06f3f3b4dd946dd5e91060afb501`
cierre_integral_BL: `NO`

Evidencia ejecutada por CODEX:

- `test_system_health.py`: **25 passed**.
- Mutación `TRACKING_ERROR_SIGMA * 1000`: **1 failed / 24 passed**.
- Mutación `TRACKING_ERROR_SIGMA / 1000`: **2 failed / 23 passed**.
- Mutación del escenario a diferencia constante (`noise=0.0`):
  **3 failed / 22 passed**.
- Restauración exacta:
  `system_health.py=e2dd4951238db5f351650eacfbde1d2a0813f953`,
  test=`483223dd92817874dba976c01bdc99bc7b1f325d`.

Cableado productivo comprobado:

- Airflow registra `control_system_health`, activo y sin pausa.
- Corridas programadas y manuales observadas en `success`; la última produjo
  `system_health.json` con DATA verde, MODEL amarillo y
  `promotions_frozen=true`.
- La llamada real a `assert_promotions_not_frozen()` sobre ese snapshot aborta
  con `PromotionsFrozenError`.
- Los eventos PSI se materializaron en `data/health/metric_events.jsonl`.

La revisión de motor, umbrales y freeze queda aprobada. BL-25 permanece
`PARTIAL`: los eventos aún usan JSONL en lugar de `control.metric_event`; las
acciones PnL `withdrawal/REDUCED` solo se publican como evento/bandera y no
tienen consumidor que ejecute la transición; la rama semanal PnL no ha sido
observada en una corrida real.
