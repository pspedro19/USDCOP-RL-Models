# CODEX handoff — 2026-07-28 08:36 COT

## Estado de corte

- Raíz saliente: `codex-root-5d968ac6`.
- Motivo: cierre ordenado solicitado por el operador; no es takeover ni bloqueo.
- HEAD observado por el monitor: `386156810ea707979223b21771501149f33cf7f2`.
- Índice Git: vacío en el último snapshot read-only.
- No ejecutar `stash`, reset, rebase, checkout destructivo ni limpieza del worktree.
- `git status --short` global no fue certificable por permisos de Git LFS; inspeccionar
  siempre paths exactos.
- Agentes nativos `review_bl02_04`, `runtime_log_monitor` y `status_monitor`: STOP.
- Auxiliar `codex-helper-7f9f3b832dd1`: ordenar STOP de `CXD-HLP-011`; cualquier
  reporte tardío se procesa en la próxima sesión, sin autoaprobación.

## Progreso estricto

- DONE global confirmado: `1/47` (`BL-07`).
- BL-07: implementación `d0427d670ff50f8179a29de2b01032e0324202e7`,
  aprobación Claude `CLD-118`, cierre `d9fe3bf`.
- Los demás BLs siguen en alguno de estos estados: implementado pendiente de
  cross-review/cierre, rechazado con remedio, parcial, bloqueado o no iniciado.
  No inflar DONE por tests verdes aislados.

## BL-10 — contenido listo, cierre administrativo pendiente

El contenido Codex quedó capturado accidentalmente dentro del commit Claude
`b86083e12d5e0a0b98cbef523fdb913518c253c4` (parent
`cfcd5a32cc2fbe1ccf675047b6c1ca4fe90fc505`) junto a BL-02/03/04.

Rutas BL-10 exactas dentro de ese objeto:

1. `.claude/specs/planes/backlog/BL-10-backfill-legacy-estimate-ft.md`
2. `registries/README.md`
3. `registries/families/usdcop_direction.yaml`
4. `registries/ledger.jsonl`
5. `scripts/validation/check_trial_ledger.py`
6. `tests/regression/test_bl10_legacy_estimate_contract.py`

Evidencia ya obtenida:

- focal final: `14 passed`;
- ledger: `239` globales, `55 FT`, `184 AT`, USD/COP `111`;
- hash-chain, counters y families: verdes;
- manifests: `19 passed`;
- scripts-layout: `20 passed`;
- frontmatter: `47 failed` idénticos a baseline, delta `0`;
- `FT-0054/55` son documentación de trials ya cobrados, sin decisión de modelado.

Incidente `CXD-045`: la aprobación Git tardía ejecutó el staging después de expirar
el lease 07:32; b860 barrió el índice a las 08:10; la renovación 08:11 fue posterior.
No atribuir falsamente una violación del lease 08:11 a Claude. Ambos lados omitieron
el candado final de scope exacto.

Próximo procedimiento, sin reescribir historia:

1. Leer ACK/objeción Claude a `CXD-045`.
2. Confirmar índice vacío y que ninguna ruta BL-10 cambió después de b860.
3. Tomar lease corto de índice y lease del pack.
4. Reemitir `reviews/BL-10.md` como sección append-only: owner CODEX,
   implementation object b860, diff limitado a las seis rutas, incidente y pruebas
   sobre snapshot limpio.
5. Pedir a Claude cross-review únicamente de esas seis rutas.
6. Solo si aprueba: cambiar MD a `IMPLEMENTED`, actualizar PROGRESS cofirmado y
   liberar leases. No duplicar el contenido en otro commit.

## Veredictos cerrados en este ciclo

- C-005/BL-13@`b808ab2...`: RECHAZADO (`REPORT CXD-HLP-009`).
  SPX quedó trackeado, pero cuatro hashes LF/CRLF y pack/contrato stale impiden
  APPLIED; BL-43 sigue bloqueado.
- BL-39@`776990a...`: RECHAZADO (`REPORT CXD-HLP-008`, `CXD-041`).
  Pack no reproducible, gate ausente de CI, artefacto `latest` mutable, 25-vs-23,
  schema/causalidad/unidades/trial inválidos.
- BL-12-r2@`6bbfd6e...`: RECHAZADO. El hash sella ledger 237/USD-COP109 frente a
  header111; guard de prosa evadible; pack stale; atribución EXP-DIR no sustentada.
  BL-10 corrige hacia adelante, no valida retroactivamente ese SHA.
- BL-15@`91fe7b6...`: RECHAZADO/PARCIAL (`REPORT CXD-HLP-010`). Pasa
  7+29+18 y existe gate por fila, pero mixed timezone filtra `TypeError`, TS acepta
  fechas imposibles sin runner, el CSV descarta el contrato validado, no existe
  ingest wall real y pack/estado están stale.
- BL-02/03/04-r2@`b86083e...`: RECHAZADO en snapshot limpio, excluyendo BL-10.
  `20 pytest + 12 Vitest` pasan, pero el texto zoo es falso para Gold/BTC
  rule-based, BTC carece de render/mutación contra early-return y la columna `✓`
  comunica acierto con símbolos/color sin nombre ni `Sí/No` accesibles.

## Entregas nuevas que requieren re-review

En este orden:

1. C-004-r4@`4c40dbbce17dc89aa93089518486d19c78f8abe8`
   (`CLD-134`): fixture único+SHA, ISO/calendario bilateral y JSON closed-world;
   verificar antes de ACK.
2. C-006/BL-20-r2@`57c3e1cb0558e5dd1f27651d7c5ba119d5603e90`
   (`CLD-133`): helper recibió `CXD-HLP-011` pero se cierra; retomar su reporte o
   relanzar snapshot. TreeSHAP está declarado PARTIAL, no COMPLETE.
3. BL-13-r4 + BL-39-r2@`386156810ea707979223b21771501149f33cf7f2`:
   llegó después del acta Claude y después del último review; revisar hashes
   canónicos, pack, C-005 y requisitos completos de BL-39.
4. BL-14@`5a2cf5d+ecbfca5`, BL-25@`254ce8f`, kafka@`3a42a48`,
   BL-01r2@`aa25516`: siguen pendientes de veredicto/confirmación.

## Cola Codex al reanudar

1. Recuperar protocolo: leer `PROTOCOL.md`, `ASSIGNMENTS.md`,
   `CLAUDE-STATUS.md`, ambos inboxes, `CONTRACTS.md`, `KNOWLEDGE.md` y este handoff.
2. Autenticar una única raíz con nuevo `instance_id`; no asumir sucesión silenciosa.
3. Cerrar incidente/pack/cross-review BL-10.
4. Procesar re-reviews P0 nuevos antes de implementar.
5. BL-16: GO solo para mecánica TDD/matriz `96→26` después de BL-13; decisiones de
   autoridad/serialización siguen NO-GO.
6. BL-43: bloqueado hasta C-005 APPLIED reproducible.
7. BL-41/C-007: no DDL/cutover hasta Vault real, roles no-super, lock/preflight,
   TDD y cut coordinado.
8. Continuar lote propio restante según dependencias; toda decisión de modelado se
   escala y no se infiere de resultados.

## Runtime al corte

- Scheduler `unhealthy`, streak `49`; pico `1557% CPU`, `166 PIDs`.
- Watchdog forecasting PID contenedor `166525`, PPID 1, inicio `13:00:16Z`,
  ~33 minutos y ~1211% CPU; log seguía progresando, sin completion/gate al STOP.
- Postgres, webserver y dashboard seguían healthy; sin recurrencia reciente de
  `week` ni auth failures.
- No matar ni reiniciar. DONE-WHEN: PID termina, log muestra completion+gate
  phase2, scheduler vuelve healthy/streak0, CPU/PIDs bajan y no hay traceback.
- Warnings a revisar como diagnóstico, no decisión de modelado: predicciones
  constantes repetidas en XGBoost/LightGBM/CatBoost y caps de scale.

## Regla permanente del incidente

Nunca dejar un permiso Git pendiente más allá del TTL. Tras cualquier pausa de
aprobación: cancelar/releer coordinación, verificar reloj+lease+índice, y ejecutar
`diff --cached --name-status` exacto inmediatamente antes de commit. Un commit
compartido nunca usa “todo lo staged”; valida una allowlist de rutas y falla cerrado.
