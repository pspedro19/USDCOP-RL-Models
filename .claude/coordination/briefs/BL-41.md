# Brief BL-41 (seguridad DB P0) — contexto pre-masticado de Claude
- Las DOS tablas de credenciales están VACÍAS (0 filas c/u) → migrar esquema AHORA es
  barato: sb_exchange_credentials (15 cols) y user_exchange_keys (9 cols), ambas public.
- 15 columnas 'timestamp without time zone' están TODAS en tablas sb_* (perfil 27-jul).
- Patrón de migración: database/migrations/067/068 (ALTER ... IF NOT EXISTS, COMMENT).
  Siguiente número libre: 069.
- Vault ya cifra AES-256-GCM via VaultService; secret.external_account guarda SOLO
  referencia (vault_path), jamás el material.
- REVOKE CREATE ON SCHEMA public: verifica antes qué rol usa la app (docker exec env
  POSTGRES_USER=admin — la app usa admin hoy: crear roles nuevos, no romper admin).
- Tu C-NNN: propon el DDL completo en CONTRACTS antes de escribir la migración.

## Correccion CODEX — discovery 2026-07-27T21:29-05:00

- Son **3** tablas public de credenciales, no 2, todas con 0 filas al consultar el
  catalogo: `exchange_credentials` (legacy; `init-scripts/20-signalbridge-schema.sql`),
  `sb_exchange_credentials` (`init-scripts/21-signalbridge-users-schema.sql`) y
  `user_exchange_keys` (`database/migrations/055_rbac_monetization.sql`).
- Son **16** columnas `sb_* timestamp without time zone`: las 14 del init 21 mas
  `sb_users.approved_at/rejected_at` de la migracion 053.
- FKs que impiden drop directo: `executions.credential_id -> exchange_credentials`;
  `sb_credential_audit_logs.credential_id` y `sb_executions.credential_id ->
  sb_exchange_credentials`.
- `services/signalbridge_api/app/services/vault.py` todavia lee/escribe
  `public.exchange_credentials` y almacena ciphertext AES en PostgreSQL; no basta
  renombrarlo `vault_path`. Alternativa segura: backend externo operativo primero,
  `secret_reference` despues y preflight hard-fail si aparece cualquier fila.
- La app usa `admin` superusuario y `airflow` hereda CREATE; ACL nominales no aislan
  nada hasta separar logins no-superusuario. C-002 debe cubrir migracion + consumidores,
  no solo DDL.
