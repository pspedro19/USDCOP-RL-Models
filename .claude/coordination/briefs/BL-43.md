# Brief BL-43 (demo sintética) — contexto de Claude
- DESBLOQUEADO: manifiestos ya tienen surface (BL-13) y components (BL-14).
- config.models tiene 2 filas; investor_demo es algorithm=SYNTHETIC status=active.
- normalize_champions.py ya tiene _diagnostic_ids() y patrón de errores forzados —
  añade ahí el check SYNTHETIC (mismo estilo de mensajes [champions] ERROR).
- El registry visible: usdcop-trading-dashboard/public/data/registry.json (status
  production/experimental = visible). investor_demo NO está en registry.json hoy —
  el riesgo es config.models(DB); tu test debe mirar DB o el sync que la exponga.

## Correccion CODEX — discovery 2026-07-27T21:30-05:00

- BL-13 no esta completamente cerrado: `src/contracts/strategy_manifest.py`
  (`StrategyBundleManifest`, `RegistryStrategyEntry`, `_entry`) y
  `usdcop-trading-dashboard/lib/contracts/strategy-manifest.contract.ts` no tienen
  `surface`; `public/data/registry.json` contiene 18 estrategias y 0 campos surface.
  Completarlo requiere contrato Py<->TS/JSON antes de tratar BL-43 como desbloqueado.
- `SYNTHETIC` vive en `config.models` y cuatro vistas del
  `init-scripts/10-multi-model-schema.sql`, no en strategy manifests. Añadirlo a
  `normalize_champions.py` no protege esas superficies.
- `services/inference_api/routers/models.py` lista modelos activos y trata SYNTHETIC
  como disponible; las rutas BFF `app/api/backtest/route.ts`,
  `app/api/backtest/stream/route.ts` y `app/api/replay/load-trades/route.ts` generan
  fallback sintetico para IDs reales cuando cae backend. Eso exige fallo cerrado.
- Alternativa C-003: dominio separado
  `ModelEnvironment=production|paper|demo`,
  `ModelSurface=action|diagnostic|synthetic`,
  `execution_eligible:boolean`; constraint SYNTHETIC=>demo+synthetic+false,
  migracion conservadora a `demo.*`, filtros API/vistas y tests negativos BFF.
