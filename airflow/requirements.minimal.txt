# MINIMAL AIRFLOW REQUIREMENTS FOR FAST STARTUP
# Essential dependencies for L0 pipeline only

# Core data processing (lightweight versions)
numpy>=1.24.0,<2.0.0
pandas>=2.0.0

# Storage and database
psycopg2-binary>=2.9.0
minio>=7.0.0

# Additional essential dependencies
requests>=2.28.0
boto3>=1.26.0
redis>=4.5.0
pydantic>=2.0.0
httpx>=0.24.0

# Data processing
pyarrow>=12.0.0

# Trading specific (lightweight)
yfinance>=0.2.0

# Time series - basic only
statsmodels>=0.14.0

# Timezone handling
pytz>=2023.3
python-dateutil>=2.8.0

# NOTE: Heavy ML dependencies (scipy, gymnasium, torch, stable-baselines3, scikit-learn) 
# are commented out for faster builds. They can be installed later if needed.
# Uncomment the following lines if ML functionality is required:
#
# scipy>=1.11.0
# gymnasium>=0.29.0
# torch>=2.0.0
# stable-baselines3>=2.0.0
# scikit-learn>=1.3.0