# CRITICAL: Essential dependencies to fix DAG import errors
# Quant runtime is pinned to the environment that produced the registered
# USD/COP directional evidence. Floating these versions changed live H1
# probabilities (sklearn 1.9 vs research 1.5.2) and broke model parity.
scipy==1.12.0
gymnasium>=0.29.0
numpy==1.26.4
pandas==2.2.1

# ML frameworks (minimal versions to avoid conflicts)
torch>=2.0.0
stable-baselines3>=2.0.0
scikit-learn==1.5.2

# Storage and database
minio>=7.0.0
psycopg2-binary>=2.9.0

# Additional common dependencies
requests>=2.28.0
boto3>=1.26.0
redis>=4.5.0
pydantic>=2.0.0
fastapi>=0.100.0
httpx>=0.24.0

# Data processing
pyarrow>=12.0.0
openpyxl>=3.1.0
xlrd>=2.0.1
pypdf>=5.0.0
olefile>=0.47

# Trading specific (compatible with Python 3.11)
yfinance>=0.2.0

# Time series analysis
statsmodels>=0.14.0

# News Engine dependencies (required for news_daily_pipeline DAG)
feedparser>=6.0
vaderSentiment>=3.3

# Forecasting model dependencies
xgboost>=1.7.0
lightgbm>=4.0.0
catboost>=1.2.0
