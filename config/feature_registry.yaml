# ==============================================================================
# FEATURE REGISTRY - derived view of the ACTIVE experiment SSOT
# ==============================================================================
# AUTHORITY CHAIN (fixed 2026-07-21, audit FEAT-001/FEAT-P0-01):
#   config/experiment_ssot.yaml  ->  this file  ->  consumers
#
# This file previously called itself "Single Source of Truth" while declaring 15
# dimensions from a SUPERSEDED generation (atr_pct, adx_14, time_normalized...) --
# features that do not exist in the active 20-dim set. EXP-B-001 (2026-02-03)
# moved the system to 20 features and updated experiment_ssot.yaml; this file was
# never regenerated, so validators built on it would reject a valid 20-dim
# dataset or wave a legacy 15-dim one into production. Feature order is
# POSITIONAL: a mismatch does not raise, it feeds the model the wrong column in
# every slot and still returns a number.
#
# There is exactly one authority for the active feature set: experiment_ssot.yaml.
# Edit THAT, then regenerate this view. Do not hand-edit the order below.
#
# Contract Version: 3.0.0 (EXP-B-001 alignment)
# Observation Dimension: 20 (18 market + 2 state)
# ==============================================================================

_meta:
  version: "3.0.0"
  created_at: "2025-01-14"
  updated_at: "2026-07-21"
  description: "Derived view of experiment_ssot.yaml - 20 dimensions (18 market + 2 state)"
  contract: "CTR-FEATURE-001"
  derived_from: "config/experiment_ssot.yaml (EXP-B-001)"
  supersedes_order: "15-dim v2.0.0 (atr_pct/adx_14/rate_spread/time_normalized generation)"

# ==============================================================================
# Observation Space Definition (regenerated from experiment_ssot.yaml)
# ==============================================================================
observation_space:
  total_dimension: 20
  market_features_count: 18
  state_features_count: 2

  # Canonical order - regenerated from experiment_ssot.yaml, do not hand-edit
  order:
    - log_ret_5m
    - log_ret_1h
    - log_ret_4h
    - log_ret_1d
    - rsi_9
    - rsi_21
    - volatility_pct
    - trend_z
    - dxy_z
    - dxy_change_1d
    - vix_z
    - embi_z
    - brent_change_1d
    - rate_spread_z
    - rate_spread_change
    - usdmxn_change_1d
    - yield_curve_z
    - gold_change_1d
    - position
    - unrealized_pnl

