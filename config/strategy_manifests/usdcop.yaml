# MANIFIESTO INMUTABLE DE ESTRATEGIA (CTR-STRAT-MANIFEST-001, Fase 0 plan 2026-07-21)
# Congelado al publicar. Cambiar CUALQUIER campo = nueva version + re-freeze consciente:
# el test de regresion compara code_hash contra los fuentes actuales y falla en drift.
# El reloj de anualizacion va DECLARADO porque la libreria carga dos convenciones
# incompatibles (onboard-asset: BTC sqrt365 | motor xasset: 252 tras intersect) y ninguna
# metrica puede citarse sin la suya.
strategy_id: smart_simple_v11
frozen: 2026-03-18 (v11 FROZEN; juez=forward 2026)
tz_storage: TIMESTAMPTZ instantes verdaderos (CTR-DQ-TZ-001); lectura AT TIME ZONE
  America/Bogota
session: 08:00-12:55 America/Bogota, Lun-Vie (60 barras M5)
freq:
  data: M5 + daily
  decision: weekly (senal lunes)
  execution: M5 en sesion, */30 min
clock:
  periods_per_year: 52
  label: weekly/52
  note: 'NUNCA 261: la estrategia toma <=1 posicion/semana'
model:
  kind: ml_ensemble
  members:
  - ridge
  - bayesian_ridge
  xgboost: OFFLINE-ONLY (use_xgboost:false, fail-closed; test estructural)
  retrain: semanal, ventana expansiva, scaler train-only
  features: 25
costs:
  maker_bps: 0.0
  weekend_exit_slippage_bps: 1.0
execution_lag: entrada lunes al cierre; HS/TP gestionados intra-semana
files:
- scripts/pipeline/train_and_export_smart_simple.py
- src/forecasting/enhance_v2.py
asset_id: usdcop
code_hash_sha256_16: a308ee93ac890b67
manifest_frozen_at: '2026-07-21'
manifest_version: 6
refreeze_note_v6: >-
  Re-freeze v6 (2026-07-21, goal 'implementarla apropiadamente'): plumbing de carry
  bidireccional en compute_pnl (APAGADO por default — swap_annual_pp: null en yaml;
  solo se enciende con la tasa NETA MEDIDA de H-COP-CARRY-00). Señal y numeros:
  bit-identicos con default.
refreeze_note_v5: >-
  Re-freeze v5 (2026-07-21, Puerta 1.4 del plan post-panel): fills de hard-stop
  OPEN-AWARE (stop-market real: en gap-through llena al open, no al nivel que no
  existio) + yaml hard_stop stop-market. Re-medicion: 2025 +7.66->+7.35 (-0.31pp,
  clava el -0.28pp que el auditor fin-math midio a mano), 2026 sin cambio (+3.36).
  Señal servida: sin cambio (es honestidad de MEDICION del fill).
refreeze_note_v4: >-
  Re-freeze v4 (2026-07-21, auditoria ML): purga de labels solapados en el fit del
  backtest (BUG A-1 — el backtest entrenaba con el desenlace de la semana operada;
  produccion NUNCA lo vio: el fix hace que el backtest MIDA lo servido; re-medicion
  2025 +13.05->+7.66, 2026 +2.45->+3.36) + circuit breaker con cooldown 4 sem
  (BUG A-2, no muerde en 2025/2026). Señal servida: sin cambio.
refreeze_note_v3: >-
  Re-freeze v3 (2026-07-21): (1) MIN_TRADES_FOR_STATS promovida a modulo (NameError
  que abortaba el export — bugfix, cero cambio de señal); (2) gating A2 de artefactos
  raiz por campeona (gobernanza de publicacion para v12 paralelo, Codex R5b — la
  generacion de señal de v11 es bit-identica). Trials sin cambio (N=59).
refreeze_note: >-
  Re-freeze consciente 2026-07-21 (v2): (1) enhance_v2.py gano include_xlead
  (default False -> señal v11 bit-identica; el experimento H-COP-XLEAD-01 ya
  esta contado como trial en el HYPOTHESIS-REGISTRY, delta DA = 0.0000);
  (2) train_and_export gano el gate 6 DSR trial-aware y la supresion N<20 —
  gobernanza de promocion, no toca la generacion de señal. Cero cambios de
  comportamiento en produccion; v11 sigue FROZEN, juez = forward 2026.
