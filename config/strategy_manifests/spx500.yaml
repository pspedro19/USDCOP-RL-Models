# MANIFIESTO INMUTABLE DE ESTRATEGIA (CTR-STRAT-MANIFEST-001, Fase 0 plan 2026-07-21)
# Congelado al publicar. Cambiar CUALQUIER campo = nueva version + re-freeze consciente:
# el test de regresion compara code_hash contra los fuentes actuales y falla en drift.
# El reloj de anualizacion va DECLARADO porque la libreria carga dos convenciones
# incompatibles (onboard-asset: BTC sqrt365 | motor xasset: 252 tras intersect) y ninguna
# metrica puede citarse sin la suya.
strategy_id: spx500_regime_gated_v1
surface: action
frozen: 2026-07-21 (techo research_validated); re-freeze v2 2026-07-27 (autoridad de datos)
tz_storage: daily; indice S&P 500 OFICIAL Investing id 166 (PRICE-RETURN declarado,
  sin dividendos — plan SPX §1), seed 1995-> mantenido por asset_spx500_pipeline_weekly
session: 09:30-16:00 America/New_York
freq:
  data: daily
  decision: daily sobre sesion cerrada
  execution: NEXT-OPEN (senal t opera al open t+1)
clock:
  periods_per_year: 252
  label: daily/252
model:
  kind: rule_based
  rules: MA200 x TSMOM 12-1 x exposure_ceiling(regimen), clip[0,1.5], long-only
  proxies: vix=vol realizada 21d shift(1) (VIXCLS ausente); macro_stress=z252 del
    proxy — modelo MAS DEBIL que el disenado, declarado
  retrain: NUNCA
costs:
  roundtrip_bps: 3.0
execution_lag: '_lag_weights: shift EXACTAMENTE una vez (SDD-006.6)'
files:
- src/strategies/spx500_regime_gated_v1/policies.py
- src/strategies/spx500_regime_gated_v1/engine.py
- src/strategies/spx500_regime_gated_v1/load_real.py
asset_id: spx500
code_hash_sha256_16: ea76413e60621521  # verificado 2026-07-28 metodo canonico LF (CXD-041):
  # fuentes ya LF en working tree => hash identico al blob git; sin cambio
manifest_frozen_at: '2026-07-27'
manifest_version: 3
refreeze_note_v3: >-
  Re-freeze v3 (2026-07-27, BL-13): discriminador surface:action añadido (aditivo,
  señal intacta, code_hash sin cambio — el hash cubre files:, no el yaml).
refreeze_note_v2: >-
  Re-freeze v2 (2026-07-27, directiva del operador "borrar SPY y usar solo el S&P 500
  oficial"): load_real.py cambia su FUENTE al indice oficial de Investing (id 166,
  seed 1995->, fail-closed, price-return declarado); el snapshot SPY (Yahoo TR) queda
  RETIRADO y eliminado del repo. Formula de la estrategia INTACTA (MA200 x TSMOM x
  techo de regimen; proxies vix/macro_stress identicos). Re-medicion 31 anios
  (1995-2026, 3 osos): gated Calmar 0.1439 PIERDE contra ma200 tonto 0.2115, B1'
  y B1 0.1635; DSR 0.7831; 5 gates fallados — el escenario "el baseline ES la
  estrategia" pre-firmado en S3. 0 trials (misma formula, cero parametros tocados);
  bundles v2.0.0 publicados 2025+2026; v1.0.0 (SPY 2020->) inmutable como contexto.
